{
  "name": "subtitle-transform",
  "version": "1.0.0",
  "description": "Transform various subtitle formats",
  "bin": {
    "subtitle-transform": "dist/cli.js"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "start": "npm run build && node dist",
    "build": "rm -rf dist && tsc",
    "test": "npm run build && tape 'test/**/*.js'",
    "prepare": "npm run build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^12.11.4",
    "@types/sax": "^1.2.0",
    "minimist": "^1.2.0",
    "sax": "^1.2.4"
  },
  "devDependencies": {
    "@types/minimist": "^1.2.0",
    "tape": "^4.11.0",
    "typescript": "^3.7.2"
  }
}
