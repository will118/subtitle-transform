{
  "name": "vtt-transform",
  "version": "1.0.0",
  "description": "```",
  "main": "dist/index.js",
  "scripts": {
    "start": "npm run build && node dist",
    "build": "rm -rf dist && tsc",
    "test": "npm run build && tape test/**/*.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "tape": "^4.11.0",
    "typescript": "^3.6.3"
  }
}
